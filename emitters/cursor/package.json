{
  "name": "@reliability-scaffold/emit-cursor",
  "version": "0.1.3-dev",
  "description": "Cursor emitter — reads canonical/ and writes .cursor/rules/*.mdc + .cursorrules.",
  "private": true,
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "emit-cursor": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').chmodSync('dist/cli.js', 0o755)\""
  },
  "dependencies": {
    "@reliability-scaffold/emit-lib": "workspace:*",
    "commander": "^12.1.0",
    "js-yaml": "^4.1.0",
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/nunjucks": "^3.2.6"
  }
}
