{
  "name": "@reliability-kit/scaffold-cli",
  "version": "0.1.3-dev",
  "description": "/scaffold wrapper — detects mode + tools, dispatches every applicable emitter, seeds team-shared files, prompts for the ratchet baseline.",
  "private": true,
  "type": "module",
  "bin": {
    "scaffold": "./dist/scaffold.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').chmodSync('dist/scaffold.js', 0o755)\""
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}
